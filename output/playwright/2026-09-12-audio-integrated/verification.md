# 实际音频整合验证

2026-09-12，主线程生产构建 4311，浏览器 scenedesk-upload-recovery；真实本地 OIDC、API、受限 generation/media worker、PostgreSQL、版本化存储和浏览器解码。能力显式为 audio_fixture_v1/test_fixture，两秒 PCM 技术音，不合成对白、不克隆声音，无付费请求。

前端 5f9b426、后端 c2a4288 分别整合为 878d9af/fa8a356。主线程审查共享媒体输入、固定声音解析、0080–0086、原始回执和归档、媒体/画布恢复、前端 session 与播放器；最终完整 check 93/93 通过。新迁移应用 7 个、总数 52，重复升级应用 0；已有受限身份重授和复核均通过。未修改已应用 SQL。

## 已验证业务

- **镜头音频**：实际计划 f710af66-be16-4d22-8a04-4047600582b3、任务及媒体 51eb0629-4147-494c-866e-9d1e0c02250d。计划保留原镜头修订和提示建议 r2，后续手工续写不改计划；执行响应实际提交并返回 202 后被测试脚本丢弃，刷新恢复同一已归档任务。后续只读恢复没有任何生成创建请求，媒体访问带 CSRF 和幂等身份；实际 WAV/已就绪代理解码、1512/390 布局通过，原内容版本仍为 5。数据库另确认同一计划只有 1 个 job、1 个提交 attempt、0 个 Take，原音频 192,044 字节、2,000,000 微秒、48 kHz、单声道。
- **画布音频**：计划 8a59519b-48dc-4d72-ba2b-6d783207afb5、任务及媒体 4c57c8c1-998c-4087-be35-a36df7e6617a。实际保存环境声草稿后固定计划；没有伪造对白或声音来源，没有 resolution/aspectRatio/withAudio。一次计划、一次执行；202 丢失后只读恢复。明确添加的 201 丢失后刷新不写入，明确恢复沿原 key 再请求一次，总共 2 次添加请求只建立一个结果节点 0b090092-29a2-4243-a0b1-a1eb22c20273。原草稿在添加时保留；随后明确删除来源，刷新从历史取回原音频并播放。整个画布流程 pageerror/失败 API 响应均为 0。
- **来源说明**：本次实际镜头与画布音频都是无对白/无声音参考的技术路径。固定对白、旧声音/角色默认声音与撤销来源在后端数据库和前端受控流程中已有专项证据；本次没有把技术音宣称为按这些来源合成的真实声音。

## 测试脚本修正与保留失败

首次镜头流程已经归档并恢复原任务，但主线程脚本错误等待原生 audio 标签；实际共用播放器使用 video 元素承载音频。该脚本在播放等待处失败，最终计数对象未输出，不能据此宣称整段脚本通过，见 initial-shot-selector-failure.txt。修正选择器后仅继续读取原任务，没有再次生成。

第一次只读播放检查的播放器未保持挂载，等待超时；随后读取页面时结果区处在视口底部、播放状态已关闭。保留 initial-shot-preview-wait.txt，不将其归因为 API/存储故障。脚本改为先将结果区滚入视口；下一次桌面播放已通过，但缩窄视口使预览释放，脚本仍等待旧关闭按钮而超时，见 initial-shot-resize-close-race.txt。最终先关闭旧预览再改变视口，完整只读检查通过，0 创建请求、0 API 错误、0 pageerror。没有修改产品时限或播放器释放规则。

主线程另修正通用结果提示：用户已删除来源后，历史仍应如实说明独立结果已保存，不能继续宣称原草稿存在。初次添加保留草稿的行为不变。

所有本目录截图均由主线程检查；这些是实际本地持久化与文件传输的技术验收，不是外部部署、真实模型质量或真实用户试点。完整数据库和故障媒体回归待对应提交的独立 GitHub CI；此前单独后端 54 check、音频 DB 7、图片/视频 DB 15、真实 WAV→AAC 1 的证据保留于实现说明 50。

最终复制说明检查及离屏释放也通过：重新加载原历史、播放实际音频、滚出视口后 decoder 卸载且 source 清空，0 生成/添加写入，0 API 错误。初次说明检查错误使用 exact 文本匹配，而该 Alert 同时含定位按钮，等待失败保留在 history-copy-selector-failure.txt；改为匹配同一容器中的说明后通过，没有产品行为变更。更新后的历史截图不再错误声称已删除的草稿仍存在。
