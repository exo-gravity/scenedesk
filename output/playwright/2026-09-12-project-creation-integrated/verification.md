# 项目创建：实际 API／PostgreSQL 浏览器整合

2026-09-12。产品组合 `bf42d93`：后端 `4b4cfe1` 和前端 `9a90cc2`；本机已追加应用冻结迁移 `0087`（共53项），重新授予受限角色权限并重启本任务API。生产构建与仓库检查108/108通过。使用既有本地测试身份、实际API、受限数据库运行角色及真实浏览器IndexedDB；没有模型调用或生成任务。

`verify-unknown.js` 先保留未提交字段，关闭并刷新后明确恢复；实际 POST 在服务端提交并返回201后由 Playwright 丢弃回包。页面保持原输入，刷新没有自动POST。`verify-db.ts expire` 只将这个明确测试请求的HTTP回执置为过期，保留持久创建绑定；随后 `verify-recovered.js` 明确恢复，服务端返回原项目。

两次创建HTTP请求的body、creationRequestId和Idempotency-Key完全相同。数据库最终只有1个对应项目、1个创建绑定、1个负责人、1个剧目与1个内容根；没有第二个创建效果。页面完成本机清理并进入原项目，重开创建表单为空。本轮新增测试项目已通过实际UI归档，原有试用项目没有修改，浏览器已回到原试用项目。

结果见 [results.json](results.json)、[缓存过期时核对](expired-receipt-db.json)、[恢复后核对](recovered-db.json)。DB结果中的 `expiredReceipts` 是该次脚本执行的过期更新数量；verify模式的0表示没有执行更新，不表示查询证明数据库中没有过期回执。1512／390宽度均无横向溢出，两段脚本无pageerror。已查看三张截图；两张unknown截图记录了弹窗的开启动画，半透明过渡不作为稳定视觉基线，recovered截图展示实际项目页面。稳定恢复页面及故障细节由独立前端浏览器证据补充。

首次脚本仅在旧页面内切换hash，没有重新装载新构建，又使用了包含必填星号的精确label定位，等待字段超时，发生在任何创建POST之前。保留 [首次失败](initial-stale-document-selector-failure.txt)。改为明确reload和按textbox可访问名称定位后完整通过，没有放宽业务断言或修改产品。旧页面过渡期间还观察到一次session读取500；重新装载后的实际会话、权限读取和两段业务验证均通过，未把该条旧页面错误伪装为成功请求。

此证据覆盖本机实际业务与故障恢复；本地测试身份不代表真实OIDC、真实用户试点或外部部署验收。旧客户端丢失原身份、浏览器数据被主动清除和其他设备另开表单仍受文档56的明确限制。对应的稳定UI和存储/会话故障验证已由前端单独完成，见[受控浏览器证据](../2026-09-12-project-creation/verification.md)。
