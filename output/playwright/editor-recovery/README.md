# 编辑器恢复浏览器证据

2026-09-11，实际生产构建、本机测试身份与技术夹具。脚本使用已有项目和固定 UUID，场次脚本会追加验收说明，提案脚本会创建明确的测试提案，不调用模型，也不自动采纳。

- `verify-committed-cleanup.js`：320 像素视口，真实 IndexedDB 删除事务 abort，服务器提交成功后保留回执，刷新与清理重试不重复提交，场次只增加一个修订。
- `verify-proposal-staging.js`：真实父草稿 put 事务 abort 后保留子表单；父草稿持久化成功，再令子草稿 delete 事务 abort。刷新恢复父草稿，并保持“本地保留，尚未提交”的准确提示。明确保存提案后才生成第 2 版。
- `verify-navigation-discard.js`：缺失场次、镜头、固定要求提示，移除定位参数关闭历史；模拟目标场次在内容快照中消失，追加意图与 CSV 保留，提交禁用；放弃草稿时删除失败仍提供恢复入口，重试成功。无导入请求。
- `verify-unmounted-commit.js`：暂扣已在服务端完成的响应，关闭编辑器后释放成功响应，验证命令仍记录回执；重新打开可清理，不重复业务写入。

脚本最终完整通过，页面异常数均为零。提案脚本初次在重开时选到了旧提案，已改为按实际列表中的目标身份定位。生产构建首次读取仍保留旧开发文档，已加入明确刷新和构建标记核对。这两次为验收脚本修正，不计为通过证据。

截图 01–05 逐张查看；生产构建覆盖 1366 与 320 像素宽度。缺失版本截图补等有限界面动画结束后再捕获。浏览器存储能力和测试身份可用；此处不是正式登录、真实用户或模型质量验收。
