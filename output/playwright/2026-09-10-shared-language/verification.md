# 共同语言明暗对照验证

日期：2026-09-10。只验证可交互设计预览，不是正式业务验收。

- `npm run ui:check` 和完整 `npm run build` 通过；导出图片后再次构建 Web，静态图片包含在构建产物中。UI 检查器仍按原迁移清单检查，不声称覆盖新预览全部样式或无障碍要求。
- 1512×982 视口：未选中、局部制作、助手展开三种状态分别切换浅／深色；每组九个素材节点的实际 x/y/宽/高完全一致。
- 局部输入面板与九个素材节点没有矩形交叠；未选中状态不显示局部输入。
- 输入提示词和助手草稿后切换主题，两份草稿均保留；刷新后 URL 中主题与预设状态恢复。草稿不做跨刷新保存。
- 六张截图直接从同一个浏览器页面的场次区域导出，保存在 `apps/web/public/previews/shared-language-v1/`；没有通过图像模型分别重绘主题。
- 人工查看浅色局部制作、浅色助手、浅色未选中、深色局部制作和深色助手；修正了输入面板遮住 SH05 及默认提示词高度不足的问题。
- Vite 生产构建实际绘制核对：深色画布背景为 rgb(25,27,25)，默认提示词完整可见；1366×900 助手状态没有整页横向溢出，固定画布区域允许内部滚动。
- 原业务主题未迁移；无新增依赖、模型调用、API 业务和正式画布工程。验证浏览器与临时 4312 预览服务完成后关闭，原 4311 开发服务保留。
