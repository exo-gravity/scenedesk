# 导航与核心流程原型检查

日期：2026-09-10。范围：`#/journey/` 独立设计预览。开发端口 4311，临时构建预览端口 4312；使用独立 Playwright 浏览器会话 `navigation-journey-0910`。

## 行为检查

在浏览器中逐步点击并用 UI 状态断言验证：

- 项目入口 → 第 1 集场次 → 咖啡厅；审阅 v1 意见回到 SH-04，带入意见作为提示词。
- 在参考抽屉添加旧铜钥匙；分镜／画布切换保留提示词与引用。
- 模拟生成 C 后采用与剪辑仍为 A；采用 C 后剪辑仍为 A；明确用于剪辑后才变为 C。
- 剪辑将 SH-04 从 9 秒改为 7 秒、向前移动一位；总时长从 46 秒变为 44 秒。
- 固定 v2 后保留 C、7 秒及新顺序；v1 仍为 A、9 秒与旧顺序，00:24 意见未迁移。
- 确认 v2 不会确认 v1；项目整集页可以明确引用已确认的咖啡厅 v2。
- 第 2 集旧居场次使用独立提示词；从生成作业定位咖啡厅 C，咖啡厅草稿仍在。
- 浏览器后退／前进分别回到旧居与咖啡厅，各场草稿与制作模式保留。
- 明暗切换不清除草稿。“我的工作”的意见入口准确返回 v1 SH-04。
- 画布节点指针拖动、Alt＋方向键、缩放、适应全场可操作；模式切换保留节点位置和视野。
- 导航总览可跳转；参考抽屉可以关闭并返回原镜头。

## 实際显示检查

已查看 1512×982 的项目、分镜、画布、剪辑、导航图和参考抽屉截图，以及 1366×900 的深色画布／分镜。1366 宽下无页面水平溢出；局部编辑区高度不足时内部滚动。

检查中修正：

1. `withinPortal=false` 的抽屉在 flex 容器内使用静态起点导致滑出屏幕；为覆盖层显式设置 inset，并验证开发与构建预览中都位于窗口内。
2. 画布媒体误继承分镜卡固定高度；按画布节点宽度恢复完整画幅比例。
3. 禁用按钮使用当前明暗色阶；数值输入的调整按钮与输入框保持在同一宽度内。
4. 初始画布按可用空间适应全场，切换模式保留视野。

构建预览实际检查浅／深主题主按钮色阶、抽屉定位、模式切换与窗口溢出。首次即时读取抽屉位置和颜色时碰到过渡动画中间态，等待动画后的稳定状态检查通过；未为此覆盖动画或修改主题。浏览器控制台最终无 error/warning。

## 命令与边界

- `npm run build` 通过（TypeScript + Vite）。
- `npm run ui:check` 通过；该脚本仅覆盖已有迁移文件与指定色对，不能代表本轮原型全部视觉或无障碍认证。
- 本轮没有安装依赖、接入模型、创建后台接口或数据库、执行真实审阅／导出，也没有提交或部署。
- 单次内存演示；刷新恢复示例。媒体为共用静帧，视频播放、声音、字幕、复杂画布、权限与多人协作未在本轮验证。

截图中 v2 的 C、7 秒及 44 秒来自上述点击过程；其他页面可能展示重置后的初始例子。截图不可作为实际生成效果或生产验收凭据。
