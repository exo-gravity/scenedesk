# 工作区效果图验证记录

日期：2026-09-10。设计原型，全部业务动作在内存中；未调用真实生成服务。

- npm run ui:check：通过。新增页面和 CSS 纳入既有源码规则；色彩数值检查仍仅覆盖既有指定不透明配色，不冒充本轮整页 WCAG 验收。
- npm run build：类型检查与 Vite 构建通过，未新增依赖，未提高 chunk 告警阈值。
- 使用独立 Playwright CLI 会话 workspace-v2；先查看 4311，最终截图来自本轮 4312 构建预览。
- 12 项行为／布局检查通过：中央与助手不重叠、没有模态遮罩、1366 完整素材打开会暂收助手、直接输入保持可见、SH-04 建议不误写 SH-05、追加建议保留旧文、双模式共享输入、自由草稿可选、空白取消选择隐藏输入、重选恢复草稿、1366×768 无全页横纵溢出、输入区在屏幕内。
- 补充检查：采用 C 后画布 C 标签显示当前采用；节点水平拖动约 30px；连线路径随位置变化；节点“编辑输入”使中央对应文本获得焦点。均通过。
- 最终构建浏览器控制台：0 errors、0 warnings。
- 导出 9 个最终状态：1512×982 明暗分镜／画布、助手收起、宽屏双辅助、候选并排，以及 1366×900 与 1366×768。
- 目视检查了核心分镜、明暗画布、宽屏双辅助、候选比较和低高度状态。调整了助手应用按钮的持续可见性、重复文字、分镜条编号换行及素材透明背景。

截图脚本一次因 hash 导航未重新加载页面而等错目标草稿，超时后改为显式重新加载干净示例；不是业务检查通过记录。最终重新导出时关闭截图动画，避免把明暗切换中的中间颜色作为定稿。

first-storyboard.png、first-canvas.png 是早期检查图；交付以 01–09 编号图为准。

未覆盖：真实工作室任务效率、真实视频质量、完整画布引擎、所有键盘／读屏路径、持久化／并发、生产 API 与费用。
