# 视觉方向查看器验证

日期：2026-09-10。仅验证静态效果图查看器，不代表业务创作功能或完整视觉系统已实现。

- `npm run ui:check`、`npm run build` 通过。前者仍按既有迁移文件清单检查，不能视为六张图片的对比度验证。
- 浏览器逐一切换三方向 × 两页面，六张图片均可解码，来源与选项一致；全屏画面完整处于 1512×982 视口内。
- 当前方向与页面写入 URL，刷新保留 C 的项目页；Esc 关闭全屏并将焦点恢复到“全屏比较”。
- 1512×982、1366×900、390×844 下查看器无横向溢出；窄屏只用于查看图片，并非移动创作页设计。
- 修正查看器选中边框被主题默认按钮样式覆盖的问题，选中边框实际显示为主题文字色。
- 在临时 4312 端口加载 Vite 生产构建，检查图片与选中状态实际绘制；返回视觉样板入口正常。
- 人工查看六张原始效果图及 `fullscreen-porcelain.png`、`gallery-dual-project.png`、`built-gallery-porcelain.png`。

图像表达视觉方向，精确字号、字标、个别图标及状态色仍需在选定方向后统一；原业务页面和主题未被这些提案替换。旧 4311 开发服务保留，临时验证浏览器与 4312 服务在完成后关闭。
