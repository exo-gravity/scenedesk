# CSV 提案浏览器验证

2026-09-10，真实本地 PostgreSQL、受限 API 角色、标明测试身份的 OIDC 模拟器；操作来自实际业务页面。开发服务 4311，正式构建预览 4312（只读检查，写入仍要求配置的 4311 Origin）。这是本地功能验收，不是生产部署或用户试点。

- 既有场次原有一镜，粘贴两行 CSV 并明确映射到该场次；生成提案不改变内容根。预览保留台词及 `2.500001` 秒精度。初次 [窄屏来源页](01-import-preview.png) 展示目标与固定来源。
- 修改 `01B · 折痕` 的叙事意图，未保存便离开编辑页；重新打开该项出现本地恢复提示，恢复后文字完整，见 [恢复后的表单](02-edit-recovered.png)。保存为第 2 版，原始第 1 版内容未改。
- 只勾选 `01B`，确认将新增 0 集、0 场、1 镜，见 [部分选择](03-partial-selection.png)。成功后内容从版本 8 变为 9，旧镜头原样保留，新增镜头要求使用修改后的意图。重新加载应用后 [采纳结果](04-applied-result.png) 标明 `01B` 已创建、`01C` 未采纳。
- [原始历史](05-original-history.png) 仍显示原始意图，并明确实际采纳使用第 2 版；旧修订只读，生命周期使用当前状态。
- 新建另一份「新结构」提案（内容基线 9）。只选镜头时提示缺少父项、采纳按钮禁用；全选后保留 3 个选择。
- 在另一标签页创建「第二集 · 基线复核」，内容根变为 10。原页面提交版本 9 被服务器 412 拒绝；没有创建任何提案对象，原三项选择仍在。页面随后展示 [真实差异](06-stale-baseline.png)：只新增该单集。验证过程中修复 JSONB 键顺序导致的误报，改用对象权威 revision 判断内容变化。
- 明确复核并更新基线，保存提案第 2 版后保留全部勾选；[最终确认](07-duplicate-names.png) 显示已有同名单集和场次，并明确新增独立结构。采纳后仅新增本提案的 1 集、1 场、1 镜，内容根变为 11。
- [320px 窄屏](08-mobile-320.png)：视口 320、文档宽 320、main 内容宽 313，无水平溢出；逐项创建标记和只读结果可访问。

截图均逐张打开检查。预期的旧基线请求产生一条 HTTP 412 控制台错误；开发服务器重启期间产生过断连日志，未当作正常运行通过证据。正式构建检查另行记录。

正式构建：重新加载 4312 预览，打开已采纳提案，[实际画面](09-built-result.png)保留同一份数据库内容、创建／未采纳标记和历史入口；控制台 0 错误、0 警告。最终数据库读取核对内容版本 11、3 集、2 场、3 镜；两条固定采纳记录分别使用提案第 2 版、在内容版本 9 创建 1 项和版本 11 创建 3 项。
