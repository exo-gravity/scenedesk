# 本地媒体回归部分结果

运行原命令 `npm run test:media`。Node 22、现有本地测试数据库、三个固定 digest 镜像已检查。日志保持原始失败，没有调大产品时限或重跑覆盖。

本轮在连续 Docker 控制面/存储初始化超时后，经主线程安排向精确拥有的 Node 测试 runner 发送 SIGINT。共观察到 6 条通过和 13 条失败 TAP 点（含父级）；没有最终完整 TAP 汇总，因此不声称全套通过，也不将未启动用例算作跳过或通过。5 个文件结束，第 6 个 probe 文件中断，9 个文件尚未启动，见 result.json。

源码在本次运行中继续推进了连续助手来源协议，因此本日志是本地环境失败证据，不代表最终候选精确 head 的完整验收。主线程另有无输入的 ffmpeg -version 控制探针；最终完整媒体验收应使用同样固定镜像和原命令在独立 Linux CI 运行。

自有 npm、runner、当时子测试和 Docker CLI 进程均已结束。现存 Created/Exited 媒体容器缺少此前捕获的本次进程归属证据，未按名称前缀盲删，不能宣称 daemon 资源全部清理。未动真实 demo、用户服务或模型执行开关。

media.log 已扫描连接 URI、签名 URL、会话和常见密钥格式，无匹配；文件权限为 0600。
