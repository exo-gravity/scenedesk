# 提示助手实际整合验证

2026-09-11。使用生产构建网页 4311、同源实际业务 API、持久 PostgreSQL 和独立受限 generation worker。沿用已建立的技术项目与固定镜头，明确扩展原本地测试连接，没有新建 worker 身份或启用真实模型。测试能力明确标为 fixture；本记录不代表真实模型质量或上线验收。

[实际流程脚本](verify-actual-prompt.js)完成：固定镜头和目标能力 → 创建计划 → 继续手工编辑 → 单次执行 → worker 生成原建议 r1 → 人工保存 r2 → 明确追加到原文。服务端实际提交 202 后丢弃执行回包，刷新通过 GET 找回同一个任务；服务端实际保存 PUT 200 后丢弃编辑回包，刷新后 GET 确认 r2，没有重复 PUT。拦截仅丢弃真实回执，不伪造计划、任务或建议响应。

记录 1 次 Plan POST、1 次 execute POST、1 次 artifact PUT。原始 r1 可单独读取；查询历史不覆盖当前编辑。明确追加保留计划后继续输入的手工内容与固定建议修订，刷新以及分镜／自由画布助手切换均恢复同一次输入。前后实际镜头内容、修订及数量完全相同，建议没有自动修改镜头或创建候选。

1512／1366／390 的本次创作输入均在视口内；确认弹窗和三个宽度的最终截图已逐张查看。0 pageerror、0 API 5xx；故意丢弃两次响应产生预期网络错误。窄屏仅验证本组件布局，不代表完整移动画布验收。独立前端的无能力禁用、下一次输入保留和额外冲突测试仍见 [受控传输记录](../2026-09-11-prompt-assistance/verification.md)，不与本次实际传输混算。

完整检查 69 项单元、完整数据库回归 172 项通过。整合发现原 0030 误删 ready 计划的数据库约束，已用追加 0034 修复；加入缺估算及有阻塞原因的数据库拒绝负例后，提示专项 13 项通过。0030–0034 在持久环境应用并冻结，总计 33 条迁移；再次升级零新增，API 和既有 worker 函数权限重授成功。

原始日志保存在忽略目录，摘要、散列和明确技术业务身份见 [results.json](results.json)。脚本针对本次夹具，重复运行应先明确保留旧输入并准备新输入，不能盲目重复用户操作。[PR #19](https://github.com/exo-gravity/scenedesk/pull/19) 已于 2026-09-11 合入 main（ff5bb6a）；精确提交 bc4cd830 的两组 CI（34599727920、34599759473）均通过 69 单元／173 数据库／61 媒体，无失败、取消或跳过。
