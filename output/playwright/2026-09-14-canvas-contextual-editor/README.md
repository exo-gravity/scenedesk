# Canvas editor v0.5 browser validation

This directory records the independent editor slice on the isolated local fixture. It does not use or modify the retained demonstration project. Root integration owns the main canvas assistant/API/worker/SQL evidence and resource cleanup.

Initial candidate: `e9825a1`, loaded `/assets/index-BERIa8SG.js`. Chrome CUA viewport: 1440 × 1000. `browser-initial.json` separates observed successes, two actual UI defects, a rejected reference fixture, and the subsequent browser-control interruption. It is **not** an all-green end-to-end result. `checks.json` records the targeted deterministic client checks and type checks; the later refusal classifier is not part of this initial browser bundle.

The local editor screenshot demonstrates the obscured model/specification row and must not be used as a final approved paint baseline. Focus screenshots were captured and inspected from the production page. Focus preserved the observable textarea ID/value and viewport; the CUA read-only API does not provide a persistent DOM ElementHandle, so no direct `isSameNode` assertion is claimed.

One separate no-reference technical video plan was prepared with visible fixed settings and one explicit fixture execution was requested. This exercises the existing local fixture, with no real model service or paid call. The initial report stopped when Chrome control lost attachment; the continuation below records subsequent read-only recovery of that same server outcome. No repeat execution, removal of the original pending request, or fake `Media.ready` write was performed.

Screenshots:

- [Initial local editor — row clipping](editor-initial-1440.png)
- [Focus view with explicit references](editor-focus-1440.png)
- [Reference refusal retained](plan-reference-refusal-1440.png)
- [Fixed technical video plan before the one execution](fixed-plan-focus-1440.png)

Reproduction uses the root-owned isolated fixture. The login origin is `localhost:4317`, not `127.0.0.1`, to keep its cookie separate from the user's local preview. The exact fixture object IDs are in the JSON report. Do not replay those IDs against another environment.

## Same-candidate continuation

The documented CUA fresh-tab recovery succeeded on the same Chrome browser. See `browser-followup.json`: server history recovered the one original technical-video result, actual nine-second proxy playback reached the end without media error, and an explicit confirmation materialized one independent result node (5 → 6 nodes). No second execution was submitted. The current draft was then edited beside its own fixed history; new input and old fixed prompt/settings were observed simultaneously. The original disconnected tab's local unknown record was not overwritten or treated as completed.

An explicit edit from the 390px node list enters focus and preserves input, with no page-level horizontal overflow in light/dark views. However, opening a results auxiliary at 390px exposes a separate stacking defect; `narrow-overlap-390.png` is failure evidence. The canvas media-success label also incorrectly reads “分镜提案可用”. These issues and the earlier compact footer/Escape issues await the next candidate; normal-flow success does not make the batch visually accepted.

The fresh browser tab is retained for the next-candidate checks. Temporary viewport was successfully reset to its 1920 × 958 default, theme returned to light, and zero media elements remained playing. Root owns fixture teardown; no user service/container was stopped.

## Narrow follow-up source status

After the e982 browser failures, the compact editor now keeps the complete short model/specification/prepare area together at the bottom; fixed-shot selection is available inside the specification disclosure and its nonzero count stays in the summary. Focus mode retains normal expanded flow. The owning canvas agent separately corrected Escape event ordering and auxiliary-panel stacking, including narrow-screen background isolation without unmounting the editor. These are source changes after e982 and **have not yet been validated in a rebuilt browser candidate**. Web TypeScript and the 146-file UI check passed after the footer change.

The application error classifier is also a post-e982 change: only a structurally verified first-attempt refusal at an explicitly known pre-effect boundary may release the review for a new diff. Identity conflicts and every unknown/resend path retain the original intent. Its eight targeted behavior tests pass. The root-owned media node label still requires the reported contextual correction from the script-specific success label; this editor slice does not alter that mapping.

## Current-build partial retest

`browser-retest.json` records source `5a8540a` / actual script `index-DARG20sN.js` with the old e982 fixture API. At 1440 × 1000 the no-reference compact editor controls are visible, Escape from seed closes only the specification popover, and the media success label is correct. `retest-compact-1440.png` was saved and actually inspected. The user then prioritized the conversational assistant redesign; the remaining two-reference, narrow/dark and materialization retests were not performed on this candidate.
