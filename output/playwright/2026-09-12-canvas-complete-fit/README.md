# 合法坐标全览与视口恢复：2026-09-12

最终产品提交链：`29234f9` → `dc19bbc` → `af83c8e`。最后一项将屏幕变换与节点坐标分开，收口本片发现的极值节点放大问题。分支基于 `7a1296d`，此前仅导入已有迁移 0095（`1692e71` 的原样 cherry-pick `b838ed6`）。这是一组隔离技术身份的实际 HTTP API、PostgreSQL 和生产构建验证，没有使用 API mock、客户数据、模型或媒体解码；不代表真实模型验收。

## 变更及冻结

- 全览和多选定位采用同一个正缩放下限 `0.00001`，与 React Flow、OpenAPI、数据库一致。合法坐标横跨约 2,000,000 单位，加最大节点宽度后，在该下限仍仅占约 20 像素，给最小 240 像素高的桌面编辑视口留下空间；不是任意将 10% 改为另一个会截断全览的值。
- 最小缩放显示 `0.001%`；本次远距双节点显示 `0.025%`，2000 节点显示 `1.8%`。没有显示为 `0%`。
- 0096 只放宽 zoom，保留原 viewport x/y 范围、actor 校验和独立偏好 CAS。首次应用后未再修改，SHA-256：`a411604fe4ae7f0ce230833be8bde3c8e29b35d217510fe6bf4c729d33eff4c7`。浏览器隔离数据库重复迁移 `applied: [] / total: 61`。
- **最终 0097**：节点 CanvasPoint 仍限 ±1000000，屏幕变换 CanvasViewport x/y 单独限 ±8000000（2 × 最大缩放 4 × 节点坐标上限）；web guard 同步。0097 首次应用后冻结 SHA-256 `3fca2b72160026050c86c70c0f8b127e4a545a3692729bfa0bb82989869a8529`，0096 保持原样。最终实际验证见下方补充。
- 中间定位回归：单节点位于 `(-1000000,-1000000)` 时，原居中结果约为 `(1000551.5,1000109.5,1)`，确实导致 422。追加修复在统一 `onMoveEnd` 中先将视觉 viewport 约束到原合法范围，非法回调不写偏好；随后合法回调才保存。这一步尚未扩大坐标契约，后续由 0097 单独扩展屏幕变换范围，未修改已应用迁移。

## 自动检查

- `check.log`：Node 22.22.2，`npm run check`，134/134 单测通过；包含类型、UI、生产构建和生成契约一致性检查。
- `focus-unit.log`：追加修复后视口专项 4/4，通过合法四角、最小编辑视口、全览、小百分比及约束后幂等；`focus-build.log` 和 `focus-ui.log` 为对应类型、生产构建、UI 检查通过记录。未为了追加局部修复重复全仓或容量套件。
- `db-viewport-final.log`：实际 API/受限角色数据库专项通过，覆盖最低缩放及普通缩放的精确保存读取、非法 zoom 拒绝、旧 CAS 412、直接数据库约束、跨项目无权访问 404、偏好不改变画布内容 revision。
- `db-first.log` 中既有 canvas 集成套件 10/10 通过；新测试最初误用通用 POST=201 helper，而 ensureCanvas 实际是 200，后改为明确校验 200。`db-viewport-second.log` 另保留测试将无权访问误期望 403 的失败，按原防存在性泄露合同改为 404；没有为测试改变服务端。
- `design-final.log`：文档校验通过 152 operations / 118 paths / 225 schemas / 163 examples / 1383 links。两个文档报告目录分别保留首次和最终校验，没有覆盖旧报告。

## 实际生产浏览器

`serve-fixture.ts` 在独立 schema/角色中使用实际 API 建立两个场次，再通过独立 4316 提供实际 API 与 `apps/web/dist`。登录 cookie 仅在服务端内存和浏览器 HttpOnly cookie 中，不写证据。`fixture.json` 只有本地技术身份、内容和几何信息。`verify-*.js` 记录本次固定 fixture 的实际 URL/ID；重建 fixture 时需用新的 manifest 更新脚本常量。

`browser-final.log` / `results.json`：

| 场景 | 实际节点/引用 | viewport | 已保存 zoom | 屏幕包围盒越界 |
|---|---:|---|---:|---:|
| 两个节点位于坐标正负极值 | 2 / 0 | 1423 × 602 | 0.0002509772865555667 | 0 |
| 20 列 × 100 行，间距 380/280 | 2000 / 5000 | 1423 × 602 | 0.017992186659976345 | 0 |

2000 场景另有 200 个组，节点为 1000 文本 + 1000 图片草稿，复用原容量布局，不含实际媒体；其负载不等同历史 42 的混合媒体容量夹具。脚本检查每一个实际 DOM 节点矩形进入画布范围，并观察到全部 5000 条引用渲染。真实 GET 保存后，刷新和“分镜 → 自由画布”均恢复原全览；100%、放大、缩小和真实鼠标平移可执行。画布 documentHash/revision 不变，流程没有内容或生成提交。

截图已目视检查：`fit-2-1512.png`、`fit-2000-1512.png`。极远双节点缩小后低于一像素，截图近似空白是全览比例的结果；“节点列表与键盘定位”仍可明确选中并恢复到可读尺度，不能仅凭这张图声称内容可读。

`browser-focus-boundary.log/json` 保留修前实际 422；`browser-focus-boundary-fixed.log/json` 证明修后只有合法 `{x:1000000,y:1000000,zoom:1}` 被该请求发送并得到 200。停留超过 1.1 秒后刷新，保存值、视觉 transform 一致，选中节点完整可见。`focus-boundary-after.png` 已目视检查。`browser-focus-fixed.log/json` 另验证修后双节点“适应内容”、Shift 多选与“定位当前内容”，以及 390 像素下原列表模式无横向溢出；截图为 `multi-focus-fixed-1512.png`、`list-fixed-390.png`。

首次浏览器脚本在异步保存尚未被随后读取确认时提前断言失败（`browser-first.log`）；直接 GET 随后确认保存成功，最终改用显式 GET 轮询。第二次等待条件将序列化 CSS scale 与数据库完整精度比较得过严（`browser-second.log`，例如 `0.0179922` 对 `0.017992186659976345`），改用原几何校验的 `1e-6` 容差，未放宽全部节点入视口或保存成功的要求。失败记录保留。

## 性能事实与剩余边界

最终浏览器是在已访问过的页面上测得点击到边界确认耗时 27 ms / 115 ms；包含自动化开销，不是冷启动或统计 P95。真实鼠标往返平移记录了变化的 viewport（两组各 21 个不同 transform）：

| 场景 | RAF 样本 | P50 | P95 | 最大帧间隔 | 交互区间 |
|---|---:|---:|---:|---:|---:|
| 2 节点 | 41 | 8.3 ms | 10.0 ms | 10.2 ms | 341 ms |
| 2000 节点全量可见 | 102 | 9.8 ms | 67.2 ms | 326.4 ms | 3214 ms |

这是有限本机交互观测，不是空闲 RAF，也不是性能优化验收。2000 节点完整全览仍有长帧；没有将文档针对 300 节点的建议 P95 当作 2000 节点硬门槛，亦没有宣称该指标达标。

中间版本曾保留 viewport ±1000000，仍会限制极值节点在 400% 时居中。本片后续已通过独立屏幕变换契约与追加 0097 收口，节点坐标不变；该旧限制不再是最终版本的剩余问题。

`requests.json` 记录本次服务的 751 次实际 HTTP 请求；非 GET 为 32 次 presence PUT、5 次成功 preference PUT，以及修前边界复现的 1 次 preference 422。无内容/任务写入。`server.log` 确认关闭独立服务并清理隔离 schema/角色；浏览器会话已关闭，4316 已释放。没有触及主线程服务、数据库夹具或浏览器。

## 最终 0097：极值节点 400% 的实际验证

`af83c8e` 后的 `translation-unit.log` 为 5/5 几何测试；`translation-db.log` 为实际 API/受限数据库专项通过，新增 ±8000000 / ±4000000 平移精确保存读取、超过视口范围的 API 与直接 SQL 拒绝，同时节点位置 1000001 仍被拒绝。`translation-build.log`、`translation-ui.log`、`contracts-translation.log` 和 `translation/design.log` 分别记录类型/生产构建、UI、生成物与最终文档检查通过。没有重跑 2000 节点性能。

新的实际隔离 schema 仅建立两个极值节点；`translation/fixture.json` 确认应用全部 62 项迁移后，重复应用新增 0 项。`translation/verify-translation.js` 通过节点列表明确定位到目标中心，再用原“放大画布”按钮达到 400%。两种节点的几何中心均留在 1423 × 307 画布内，停留超过 1.1 秒后刷新，GET 与视觉变换一致：

| 节点坐标 | 最终保存 viewport x / y / zoom | 刷新后锚点屏幕位置 |
|---|---|---|
| -1000000 / -1000000 | 4000071.499999999 / 3999977.4999999995 / 4 | 752.5 / 438.1953125 |
| 1000000 / 1000000 | -3999928.4999999995 / -4000208.499999999 / 4 | 752.5 / 436.5703125 |

见 `translation/results.json` 和 `browser-verified.log`：最终一轮 4 次相关偏好 PUT 全部 200，无 pageerror；最后点击“适应内容”仍让两个节点完整进入视口。400% 时节点本身可能大于视口，因此核对的是锚点可见与恢复一致，没有要求整个放大节点包围盒都在视口内。`zoom4-negative-1512.png` 与 `zoom4-positive-1512.png` 已目视检查。

`translation/browser-first.log` 保留最初滚轮步骤超时：指针位于既有 `nowheel` 文本区域，没有达到目标缩放。随后使用现有工具栏放大按钮，未修改产品滚轮语义。`browser-final.log` 保留脚本最初硬要求至少 4 次 PUT 的断言错误；该次开始时第一节点的 focus 已保存，正常无需再次写入。最终校验改为两种符号的 400% 偏好都确实发出成功请求，且所有写响应成功，没有要求对未变化状态额外写入。服务端完整记录可确认所有轮次均无偏好错误响应。

`translation/requests.json` 总计 320 次实际请求，非 GET 仅 13 次 presence PUT 和 10 次 preference PUT，全部 200。新旧两个自有 schema/角色均已清理，两次 `server.log` 都有 `FIT_FIXTURE_CLEANED`；独立浏览器会话已关闭，4316 再次释放。
