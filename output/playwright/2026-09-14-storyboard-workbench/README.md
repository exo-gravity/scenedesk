# 分镜台真实浏览器验收

2026-09-14。本地完整服务 `http://127.0.0.1:4311`，真实项目「旧钥匙 · 完整演示」。IAB 只读查看；Chrome 使用本地身份测试本机草稿并恢复原值，不新增业务数据。页面来自生产构建，不是静态原型。

| 场景 | 实际结果 |
|---|---|
| 命名与布局 | 画布 / 分镜台顶部居中；默认输入收起，真实视频完整画幅；1440×900 主视频约 501px 高 |
| 草稿 | SH-04输入→立即下一镜→上一镜→画布→分镜台→刷新，文案完整保留；测试后恢复原空值 |
| 候选 | 查看候选1，页面仍标明实际采用候选2；打开确认采用后取消，未执行采用 |
| 固定参考 | 显示该候选固定要求v1的真实媒体；打开聚焦面板关闭按钮，Esc返回 |
| 全场总览 | 六个真实镜头；主制作区渲染高度0；焦点进入返回按钮，Esc返回总览入口 |
| 媒体 | 播放数1→打开总览后0，暂停在0.399826秒而非重置 |
| 镜头导航 | End到SH-06，下一镜禁用；390px当前镜头完整位于x158至378，整页无横向溢出 |
| 辅助面板键盘 | 素材范围展开后第一次Esc只关闭下拉框，第二次关闭面板并回到「新建候选」 |
| 同候选返回 | 候选1→另选区间编辑→再次点候选1，回到预览且仍采用候选2 |
| 桌面助手 | 1440px下模式中心x720，右栏x1080至1440、y64至900，打开不偏移 |
| 窄屏助手 | 可展开满宽对话栏并收起，未选择节点也保留可输入对话框；未发送消息 |

截图：

- `before-1280.png`：优化前常驻参考、浮动输入卡与被挤小的视频。
- `desktop-1440.png`：完整画幅与紧凑输入、镜头导航。
- `candidates-1440.png`：真实缩略图、候选名称、观看与采用区别。
- `references-1440.png`：按需固定参考面板。
- `overview-1440.png`：全场总览与独立返回。
- `mobile-390.png`、`mobile-assistant.png`：窄屏布局与对话侧栏。
- `dark-1440.png`、`assistant-1440.png`、`creation-1440.png`：深色、贴边助手与960px有界输入。

构建与代码检查：`npm run check` 200/200通过，TypeScript/UI/合约通过；CSS最终打包通过。原有大模块bundle提示仍在，不是失败。未修改媒体、候选、采用等创作事实；只有本机草稿和工作区视图偏好变化，没有付费模型或数据库迁移，也没有把浏览器检查扩称为真人体验或模型质量验收。
