async(page)=>{
 const cdp=await page.context().newCDPSession(page);await cdp.send('Performance.enable');const preGC={dom:await cdp.send('Memory.getDOMCounters'),metrics:await cdp.send('Performance.getMetrics')};await cdp.send('HeapProfiler.collectGarbage');const postGC={dom:await cdp.send('Memory.getDOMCounters'),metrics:await cdp.send('Performance.getMetrics')};await cdp.detach();return {case:__CASE__.nodeCount,preGC,postGC,view:await page.evaluate(()=>({dom:document.querySelectorAll('*').length,flowNodes:document.querySelectorAll('.react-flow__node').length,flowEdges:document.querySelectorAll('.react-flow__edge').length,details:[...document.querySelectorAll('details')].map(d=>({open:d.open,elements:d.querySelectorAll('*').length})),players:document.querySelectorAll('video,audio').length}))};
}
