# 创作恢复整合验证

2026-09-12，独立工作树 `feat/recovery-browser-integration`。以已整合音频与私有部署的 `c9233f60bce57a2a7d6eddc4341b3ea62133dff8` 为基础，依次 cherry-pick `cc0a14b` 与 `8e02af7`；实际验证组合提交为 `900d22035de0c923bd51d7db58d12563c6291584`。本证据提交只新增此目录，不再次修改产品代码或历史证据。

## 检查

- `npm run check` **99/99 PASS**，包括契约一致性、UI 检查、完整 TypeScript 检查、Vite production build 和单元行为测试。完整输出为 `check.log`。
- 在独立 4316、`scenedesk-ai-ui` 浏览器验证这次生产构建。`verify-creation.js`、`verify-workflow.js`、`verify-mobile.js` 与 54／53 原脚本相比，**仅修改证据输出目录前缀**。已按文件内容比对确认，原接口、假身份、交互步骤、成功与失败断言都未改动。
- 三段浏览器脚本本次均第一次执行通过。`creation-result.json`、`workflow-result.json`、`mobile-result.json` 保留实际返回结果。

## 创建恢复

单集、场次、镜头各自先将请求提交到受控服务状态并缓存幂等回执，再故意中断响应；刷新内容树已包含新对象，版本已经变化。恢复原输入后没有重新设基线或丢弃未知请求入口，普通创建不可用；明确恢复原请求仍使用相同正文、位置、If-Match、Idempotency-Key。**每类 2 次请求，只有 1 次创建；刷新自动 POST 为 0。**

继续验证发送前 IndexedDB 写失败为 0 次 HTTP 请求；首次明确 412 后保留输入、显式返回编辑和核对新基线；服务器成功后的本机清理失败可重试／刷新清理，不会重建对象或复活旧请求。全段共 10 次创建请求、6 次受控创建、其他写入 0、`pageErrors: []`。

## 助手与撤权恢复

工作室 membership 始终有效。项目内容 GET 临时 503 后保留编辑器原文；项目 GET／内容 GET 确认 403 后隐藏项目旧数据、标题中的项目名、编辑器和创建入口。拒绝后的 503 不重新展示旧缓存，本机字段草稿保留；恢复授权后可明确还原原文。

镜头第 1 版执行提示准备后，第 2 版成为当前来源，未应用任何建议也能明确“保留当前输入，另开一次”；确认框固定第 2 版，刷新后保留第 1 版原手工提示、准备要求、固定来源和旧计划／任务。**计划 POST 1，任务 POST 1，其他写入 0，`pageErrors: []`。**

## 视觉与范围

已逐一检查本目录全部截图：集场镜恢复和已提交后清理状态，临时故障下的草稿，撤权隐藏状态，新修订确认框，刷新后旧输入，以及 390 px 的创建／助手保留内容。窄屏恢复入口与历史输入可见，原宽度断言通过；页面包含最新私有入口与图／视频／音频生成组合，没有为了验收恢复功能而改回旧导航。

所有 `/v1`、`/health` 响应均为明确受控 transport，浏览器表单、Query、IndexedDB、刷新、渲染是真实执行。这份证据不代表实际 API／PostgreSQL 的创建幂等验收或真实模型验收；没有写主线程项目、操作主线程服务、运行 DB／媒体套件或调用付费服务。24 小时回执过期和未纳入的项目创建边界仍按 54 的限制记录，由主线程独立核验真实服务。
