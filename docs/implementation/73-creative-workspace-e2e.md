# 创作工作区端到端与设计验收

2026-09-16。用户要求核心设计与页面做端到端测试。本规范落实在[重构计划](70-creative-workspace-refactor.md)的每个业务切片，沿用[已确认设计](../design/creative-workspace-approved-2026-09-16.md)，不等最后一个 PR 才回归。测试不是新增产品功能；未实现的用例不能用假页面标记通过。

## 合并条件

每个核心页面 PR 同时提供以下证据：

1. **实际业务流程**：浏览器操作正式构建，连接实际 API、受限数据库角色及适用的存储／队列。断言用户可观察的结果，并通过刷新、另一个会话或公开读取接口验证持久化。不能把全部业务接口替换成成功响应再称为端到端。
2. **故障恢复**：覆盖本次修改涉及的响应丢失、冲突、刷新、退出、权限和归档边界。局部故障注入必须注明；恢复后确认没有重复业务效果、草稿丢失或固定引用漂移。
3. **设计与交互**：查看生产构建的真实截图和操作，对照已确认页面的层级、工作区占比、工具位置、状态反馈及上下文连续性。类型检查、DOM 存在、无横向溢出和截图文件生成都不能单独证明设计验收。
4. **自动回归**：稳定主路径进入仓库 E2E 套件并在 CI 执行；后端权限／事务／供应商状态的组合边界继续由数据库和媒体测试覆盖。自动回归尚缺失时明确记录，不用一次手工检查关闭其缺口。

由负责该 PR 的实现者记录用例、提交和结果，集成时核对真实差异及证据。失败需要修复或明确缩小交付范围；不能通过删断言、盲目更新截图或改成始终成功的 mock 解除门槛。会产生费用的真实模型验收按已授权服务和额度独立执行，CI 默认不访问付费供应商。

## 主流程验收矩阵

编号属于本轮回归，不替换既有 AT 编号。下表为需要交付的行为，执行状态以各 PR 的证据记录为准。

| 用例 | 浏览器路径与可观察结果 | 必须验证的持久化／失败边界 | 所属切片 |
|---|---|---|---|
| CW-01 项目导航 | 项目卡片进入剧本；侧栏剧本／画布／项目资产可达；资产详情仍有项目上下文 | 旧场次、制作、媒体及固定修订链接可达；读取失败不显示旧项目缓存 | PR-1 |
| CW-02 侧栏与输入连续性 | 桌面折叠、窄屏抽屉、键盘关闭和焦点返回；从页面跳转后能继续输入 | 剧本未提交草稿跨页面及刷新可恢复；画布／助手导航经过保留屏障 | PR-1、2、5 |
| CW-03 空项目开始 | 无集场数据的项目直接打开画布，创建并编辑节点，重开仍在 | 没有隐式创建 Scene；并发打开得到同一项目画布；个人视口不覆盖他人 | PR-2 |
| CW-04 新旧画布兼容 | 项目画布与旧场次画布各自打开，原媒体、引用、任务和位置可找回 | 项目／场次作用域不串数据；固定旧计划保持原输入；只读会话不可写 | PR-2 |
| CW-05 Word 预览与导入 | 从文件选择器选 `.docx`，核对标题、对白、表格及图片，明确导入后阅读 | 预览不等于提交；解析限制有说明；损坏／超限文件不产生成功修订；错误后保留可恢复输入 | PR-3 |
| CW-06 剧本历史与共享 | 下载原件、阅读新旧版本，另一有效成员可查看；固定选文可作为创作依据 | 新导入追加不可变修订；旧选文不随最新版改变；撤权后正文与原件都不可读取 | PR-3，引用集成接 PR-2 |
| CW-07 导入提交恢复 | 提交后丢失响应、刷新后核对导入结果，再继续工作 | 同一意图只生成一个修订；旧确认不能覆盖后来版本；不得将本地保留失败标成完成 | PR-3 |
| CW-08 飞书正文 | 实际授权链接导入站内正文，显示来源与时间；明确更新 | 失效／撤权／超时可恢复；只保存链接不能冒充导入正文；旧固定引用保持有效 | PR-4 |
| CW-09 图像创作 | 选引用／参考、输入、生成、查看实际归档结果、比较并继续修改 | 执行使用固定输入；运行中可编辑下一稿；完成不覆盖下一稿、不自动采用 | PR-5、7 |
| CW-10 视频创作 | 在同一工作区设置实际支持的参数，生成、播放、比较并继续修改 | 模式／参考传输正确；结果是可播放归档媒体；海报不冒充视频完成 | PR-5、7 |
| CW-11 生成中断恢复 | 重复点击、响应丢失、关闭重开、查询／归档失败后回到原任务 | attempt 先持久化；未知提交不自动重购；归档重试只归档，不创建新付费任务 | PR-5、7 |
| CW-12 结果找回 | 原草稿修改／删除后从任务或历史找回已完成结果，再明确放入画布 | 固定生成历史保留；同一放置意图不重复加节点；撤销不被后台自动重加 | PR-5 |
| CW-13 镜头整理 | 画布素材加入镜头列表，排序、专注预览、播放、比较和明确选用 | Selection、Take、Media 保持独立；整理不强迫用户切到第二套创作工作台 | PR-6 |
| CW-14 助手应用 | 讨论／引用／建议／明确应用后回到同一草稿，继续修改；切换后能找回上下文 | 同一命令和授权边界；旧建议按原对象／版本复核；失败不丢输入、不越过确认 | PR-5、6 |
| CW-15 交付 | 按明确选用下载／导出，核对可打开的实际文件与内容 | 对应确切媒体／版本，未选用结果不混入；失败不显示完成 | PR-6 |
| CW-16 完整试作 | 同一剧本导入→引用→图片／视频创作→比较→镜头整理→选用→导出 | 记录中途退出后继续、协作者阅读、操作数量及剩余绕路；真实模型样片单列 | PR-8 |

## 共用负例与设计检查

这些检查随影响范围选取，不要求每个小改动重复执行无关组合：

| 范围 | 必须保留的断言 |
|---|---|
| 访问与权限 | 当前 tenant/project 权限决定读写及原件下载；跨项目 ID、撤权、只读和归档有明确结果；幂等命中仍检查当前权限 |
| 冲突与确认 | 第二会话更新后原确认不能静默覆盖；保留当前草稿、原目标／版本和重试所需信息；失败弹窗可退出但不误报成功 |
| 本地恢复 | 网络不可用、保存回执丢失、IndexedDB 保留失败及刷新；不在业务效果和本地恢复状态不一致时显示保存完成 |
| 执行与媒体 | 固定输入／供应商任务／归档结果分开；unknown 不触发新付费提交；原件、预览、正式结果不混用 |
| 桌面视觉 | 在约 1440px 和 1280px 宽度核对主次层级、侧栏、画布可用面积、节点输入和助手占位；真实长标题及长模型名不挤坏布局 |
| 窄屏与键盘 | 约 820px 与 390px 检查入口、抽屉、滚动、错误和确认；Tab 焦点可见、弹窗焦点管理正确；不能靠裁剪隐藏功能 |
| 状态与主题 | 明暗主题、加载、空白、只读、失败、运行中和完成状态；状态名与真实业务事实一致，不以颜色作为唯一提示 |

新 E2E 基础设施使用隔离合成项目和测试身份，测试启动和清理不会重置现有本机项目。测试专用身份引导不能进入生产部署。fixture 媒体／供应商必须明确标识；测试产物不得包含真实客户文档、可用会话令牌或私有存储凭据。

## 执行与证据记录

每条记录应包括提交、正式构建与测试配置、用例编号、实际操作／预期／结果、截图或运行报告、失败后复验以及未覆盖项。截图使用合成数据；运行产物写入新的 `output/` 子目录或受限 CI artifact，不覆盖历史证据。不以原型截图替代正式构建截图。

当前已确认的证据：

- PR-1 产品提交 `597e96f`：实际正式构建连接本机 API，完成 CW-01 的正常路径、CW-02 的导航／折叠／抽屉／剧本草稿恢复及历史阅读，检查 1440px 明暗主题、820px 和 390px。草稿恢复检查没有保存新服务器修订。键盘 Enter 打开窄屏抽屉、Escape 关闭及焦点返回亦通过。PR #42 最终提交 `1ac1f87` 全部 CI 通过，合入 `de29b08`；详见[PR-1 记录](70-creative-workspace-refactor.md)。该证据使用本机演示项目，不是真实模型验收；权限负例和自动化页面回归未在该手工记录中宣称通过。
- 独立 E2E 支持 PR 正在建立自动回归与隔离 fixture。未看到对应运行结果前，不能将本矩阵标为全绿。
- 项目画布和 Word 导入处于并行开发，CW-03～07 待相应生产切片的真实浏览器验收。飞书、连续创作、镜头整理和真实模型亦按所属 PR 验收，不因旧模块或原型存在而提前关闭。
