# 创作工作区重构：实施节奏与验收

2026-09-16。用户已授权开始落地并将验证后的变更合入。依据 [已确认方向](../design/creative-workspace-approved-2026-09-16.md)。开始时正式基线为 `1944e4b`（PR #41）；原型分支单独保留，不把内存演示流程直接接到生产入口。

## 八个 PR，三个阶段

最新执行顺序：用户要求“真实模型这个地方可以放到最后，其他的全部完成”。因此先完成 PR-1～6 和 PR-8 中不依赖真实供应商的功能、切换、自动回归与本机部署／恢复检查，PR-7 置于最后。真实飞书授权、团队文档与外部环境仍单独记录，不将受控响应或本机测试写成真实验收；原来后置的完整后期、运营和公共 API 不因此重新进入首版。

以下 PR-1～PR-8 是计划序号，不是 GitHub 编号。每个切片包含所需的页面、接口、持久化、错误恢复与验证；按真实复杂度可再拆小，不为保持数量将大迁移硬塞进单个 PR。不承诺缺少团队数据支持的精确工期。

| PR | 交付结果 | 必须通过的验收 | 依赖／状态 |
|---|---|---|---|
| 1：正式导航与兼容入口 | 一个项目侧栏，剧本／画布／项目资产，项目优先打开剧本；真实已有画布入口；场次管理下沉；旧深链可用 | 剧本、资产详情、场次及旧制作链接可达；折叠、窄屏、深色、归档与访问失败不误显内容 | 已通过本机验收与远端 CI；[PR #42](https://github.com/exo-gravity/scenedesk/pull/42) 已合入 `de29b08` |
| 2：项目级画布 | 不创建场次即可开始；画布保存、偏好、生成／助手作用域及恢复允许项目归属；接入正式工作区壳层 | 空项目直接创作、重开恢复、权限隔离、并发唯一创建；旧场次画布和旧引用继续有效 | [PR #44](https://github.com/exo-gravity/scenedesk/pull/44) 最终产品头 `590dfc7` 四项 CI 已通过，合入 `432dcfd`；迁移 0106，API／浏览器证据见[71](71-project-canvas-workspace.md) |
| 3：本地剧本导入与阅读 | `.docx` 原件、解析预览、标题与段落展示、明确导入、固定新版本、历史与项目共享阅读、选文引用 | 实际文件标题／对白／表格／必要图片不静默丢失；失败保留输入；原件可下载；更新不漂移引用；另一成员可读、无权不可读 | [PR #45](https://github.com/exo-gravity/scenedesk/pull/45) 已合入；[PR #47](https://github.com/exo-gravity/scenedesk/pull/47) 已补固定选文进入项目画布。实现和本机浏览器证据见[72](72-script-docx-import.md)／[74](74-canvas-continuous-creation.md)；团队实际 Word 版式仍需真实样本验收 |
| 4：飞书正文导入 | 支持经验证的文档类型与授权路径，站内正文、来源、导入时间和主动更新 | 实际授权文档可导入；无权限、失效链接、超时明确可恢复；仅链接状态不冒充正文；更新与旧引用独立 | [PR #48](https://github.com/exo-gravity/scenedesk/pull/48) 已合入 `36a162e`，使用团队共用应用及项目精确源绑定；受控正文与恢复已验，真实团队应用／文档授权仍待配置，详见[75](75-feishu-script-import.md) |
| 5：连续创作与恢复 | 同一草稿内参考／生成／结果／比较／继续修改；内部计划固定；执行与下一稿分离；结果就地可见 | 图片／视频受控链路、重复点击防护、未知提交不重购、刷新找回、归档重试；结果不覆盖当前草稿或采用 | #47 与 [#51](https://github.com/exo-gravity/scenedesk/pull/51) 已合入，后者为 `2af36fc`；固定视频、助手续改、刷新定位及窄屏专项已通过，真实供应商单列 PR-7 |
| 6：镜头整理、助手与交付 | 镜头列表和专注预览、排序／播放／比较／明确选用；助手作用于同一草稿；按选用下载／导出 | 连续完成一组镜头；图／视频语义正确；撤销或切换保留编辑；旧结果、固定输入和选用可找回；导出对应确切媒体 | #46、#51 与 [#53](https://github.com/exo-gravity/scenedesk/pull/53) 已合入；#53 为 `ae1380b`，本场选用原片包通过 16 项 DB、2 条浏览器及实际下载验收。普通路径进入镜头列表，旧深链兼容保留 |
| 7：首批真实模型 | 接入已核实的一个图片模式和一个视频模式，再按实际需求扩充 | 实际服务授权、输入传输、供应商状态、固定结果归档、限额与恢复；记录真实样片及消耗；不以 fixture 代替 | 按用户最新要求放到本轮非模型收尾之后；付费运行仍需实际服务、凭据和消费额度授权 |
| 8：切换与完整回归 | 默认入口切到新路径；旧数据／深链兼容；清理重复入口；团队真实试用与交付记录 | 同一实际剧本的导入→创作→整理→导出；权限／冲突／响应丢失；桌面与窄屏；操作成本与旧流程对照 | 先完成 PR-2～6 的联合受控流程、切换、自动回归和本机升级；真实模型与团队试作独立收尾，不隐藏外部条件缺口 |

阶段一（PR-1～2）：正式入口与无场次创作。阶段二（PR-3～5）：外部剧本到连续视觉创作。阶段三（PR-6～8）：整理交付与真实使用验收。PR-7 的账号、协议和输入约束提前核实，飞书条件也提前调查。

## 并行实施与合入顺序

当前收尾结果集中记录在[77 非模型工作区收尾](77-non-provider-workspace-closure.md)。下面按日期保留的“仅发现未执行”“草稿”“待合入”是当时状态；以 77 和[22 当前进度](22-implementation-progress.md)的最新实测结果为准，不覆盖或删除原失败记录。

用户随后明确要求尽可能并行开发多个 PR。已启动三个独立工作目录／分支：`feat/project-canvas-workspace` 实现 PR-2，`feat/script-docx-import` 实现 PR-3，`feat/creative-workspace-e2e` 建立核心页面自动回归；集成负责人负责 PR-1、设计及端到端验收矩阵、差异审查和合入。E2E 是支持业务切片的额外 PR，不将八个工作包误当作固定八次 GitHub 合入。

- PR-2 与 PR-3 从同一已验证导航基础开发。分别拥有画布作用域与文档导入，不共同修改同一个工作目录。
- PR-2 使用新迁移 0106–0109 范围，PR-3 从 0110 开始；只创建实际需要的迁移。迁移器拒绝晚到的低编号迁移，因此**开发和隔离测试并行，数据库合入与共用环境升级先 PR-2 后 PR-3**。
- 契约先改各自 Python 生成源；集成共享变更后重新生成 OpenAPI／TypeScript，不手工拼接生成产物。关联剧本引用到画布的完整流程在两条线集成后复验。
- 每条分支分别提交、检查、推送和建立 PR。相关 CI、实际页面及恢复用例通过后才合入；不因并行赶进度跳过串行集成。后续 PR-4/5 可分别在剧本表示和画布作用域稳定后继续并行。

## 核心页面的合并验收

用户要求核心设计与页面做端到端测试。具体用例与证据规则见[73 端到端与设计验收](73-creative-workspace-e2e.md)：每个核心页面变更必须验收真实业务操作、持久化与恢复，并查看正式构建是否符合已确认设计；稳定主路径加入自动回归。最终 PR-8 做串联试作，不承担补齐前面所有测试的职责。真实模型测试和受控 fixture 测试分别记录。

### 第二批并行进度（2026-09-16）

项目画布已由 GitHub PR #44 合入 `432dcfd`。Word PR #45 已整合该基线；后续三条独立分支共同基于 Word 的固定正文／阅读表示开发：PR #47 提供选文进入画布和单次生成／下一稿分离，PR #46 提供可选镜头列表／比较／选用／固定原片下载，PR #48 提供团队共用应用的飞书正文导入。它们是计划 PR-4～6 的首个切片，不代表这些工作包全部完成。迁移只增加实际需要的 0111（固定剧本画布来源）与 0115（飞书预览）；应用到同一数据库时先 0111 后 0115。

最新交互约束：Word 和飞书均以“预览 → 确认导入 → 当前稿”为主线；新稿导入后自动成为当前稿。历史收于次级入口，正常路径不要求填写或核对版本号；只在真实冲突／失败时显示必要恢复信息。固定正文、已提交生成输入与结果仍由后台保存，导入新稿不会静默改变旧任务依据。

合入前 PR #45–48 均为草稿，GitHub Actions 因账户付款／额度限制未启动。随后用户明确要求手动合并，本轮按下述本机验收证据完成合入；远端检查缺口仍保留，不将未启动任务标成通过。

### 本批手动合入记录

2026-09-16，用户暂时无法恢复 Actions 额度，要求由代理手动合并，亦可自行点击 Merge。查询时 `main` 返回 `protected: false`；采用普通 merge commit 和精确 head 校验，没有使用 `--admin`，没有修改保护、权限、工作流或仓库可见性。用户随后明确授权继续推进并手动合并，后续 PR 亦可在差异审查及必要本机验证后采用相同方式；没有执行的检查如实记录，相关产品失败不能当作额度问题忽略。优先由代理完成，只有实际权限需要时才交给用户操作。

| 顺序 | PR／范围 | 实际合入的 PR head | main 合并提交 |
|---|---|---|---|
| 1 | [#45 Word 导入与轻量阅读](https://github.com/exo-gravity/scenedesk/pull/45) | `ffd8861` | `e4f2767` |
| 2 | [#47 固定选文与连续创作](https://github.com/exo-gravity/scenedesk/pull/47) | `8c130b9` | `0b9e190` |
| 3 | [#46 镜头列表与固定原片下载](https://github.com/exo-gravity/scenedesk/pull/46) | `477c264` | `b4931d2` |
| 4 | [#48 团队飞书正文导入](https://github.com/exo-gravity/scenedesk/pull/48) | `9b78049` | `36a162e` |

后续三个 PR 均先改为以 `main` 为目标并吸收前置合并。最终文件树与已完成串联验收的本地集成头 `065b3c4` 完全一致，未在最后合并时引入新产品修改。飞书集成的三个共享文件冲突复用已审查的本地解法；Python 契约生成源与生成产物已联合检查。数据库新增顺序仍为 0110 → 0111 → 0115，未升级用户原有运行环境。

本机组合检查通过 258 项单元、契约／UI／类型／生产构建、9 项专项数据库测试及静态文档检查。生产浏览器串联通过飞书正文预览／导入 → 规范选文进入画布 → 镜头预览和明确选用 → 重新导入 Word → 刷新后当前稿更新且旧画布引用保持原文。证据分别保存在集成工作目录的 `output/integration/2026-09-16-wave-two-precheck-01/` 和 `output/integration/2026-09-16-local-preview-01/`。

仍未完成：25 条自动 E2E 只发现未执行；远端工作流均因计费未启动；完整媒体回归的既有嵌入音频用例超时尚未定位；部署／恢复验证待补。飞书和视频浏览器资料为合成样本，真实团队授权、付费模型和完整试作仍单列。合并代码不等于上述验收已完成。

### 合入后完整数据库回归与本机工作台升级

同一产品代码串行执行所有 `tests/integration/*.test.ts`：327 项通过，零失败／跳过，48 个顶层用例，耗时 547 秒。使用各用例独立随机 schema／受限角色，未将用户项目作为测试 fixture；日志位于集成工作目录 `output/integration/2026-09-16-merged-database-regression/database.log`。运行起点 `358239e` 与合入头 `126d8a8` 的产品代码相同。

持久化本机工作台 4311 已更新为 `126d8a8` 的正式构建。先为既有 `drama` schema 建立私有备份，再应用 0106 → 0110 → 0111 → 0115 并重新授予显式运行权限；迁移前后项目／剧本／场次／画布数量一致，未重置已有业务数据。原 API、Web、媒体及 fixture 生成 worker 更新到同一代码与原有配置，仍不启用真实模型。浏览器实际确认原登录、正文、已有场次画布、新镜头列表与 6 秒视频播放；未在用户项目中重新导入、改写正文或改变选用。原 4343 一次性 Word 预览保留。

本机升级证据位于主工作目录 `output/implementation/2026-09-16-main-workspace-upgrade/`，私有备份／进程日志留在未跟踪的 `.runtime/creative-workspace-main-126d8a8/`。数据库备份已创建，不声称做过本轮恢复演练。该本机升级不等于外部生产部署、25 条自动浏览器套件、完整媒体或真实供应商验收。

## 首阶段明确的工程选择

1. 新导航仅在项目浏览页替换原双侧栏；原制作页在 PR-2 接入同一壳层前保留原退出屏障。PR-1 的画布入口读取已有项目/集场数据，显式进入既有场次画布，不静默创建空场次或业务对象，也不声称无场次创作完成。
2. 项目卡片先打开剧本；「画布」与「项目资产」始终可达。场次管理与项目设置保留次级入口，旧 `/content`、`/production`、`/media` 和带固定修订的链接继续解析。
3. 画布本体、生成、助手和镜头领域的变化分别随所属 PR 落实，不借导航替换降低原恢复能力。正式画布侧栏的新跳转须调用现有保留屏障，不能只写 `location.hash`。
4. `.docx` 是首个本地格式；剧本默认阅读、明确重新导入。正文编辑和其他格式仍按实际需要裁决，现有历史编辑代码保留。飞书凭据在配置渠道提供，不写入项目或聊天日志。
5. 首批模型优先按用户已有使用场景评估 Seedream 图片、Seedance 视频；Kling 及其他图片模型保持后续 Adapter 接入范围。具体版本、模式、官方/聚合渠道与消费额度必须核实后记录，未在本 PR 中替用户购买或启用供应商。

## 必须同步的契约

PR-2 更新画布工程契约、领域关系、API 生成源、数据库约束和受限角色授权。PR-3/4 更新 ScriptRevision 来源、原件/显示表示/规范引用文本的对应关系，不将上传文档直接塞进既有媒体允许列表而绕过文件安全与授权。PR-5 更新生成意图、固定输入及结果展示/放置协议，查询、归档重试与新生成语义独立。PR-6 保留 Selection、Take 与 Media 之分。适用修订写在原契约对应章节及实现说明中，避免本计划成为第二套技术真相。

## 需要提前准备的真实验收材料

- 团队实际 `.docx` 样本和版式要求；至少涵盖对白、标题、表格或图片中的实际用法。
- 实际飞书文档类型、租户和应用可读取授权；验证不可读与撤权情形。链接本身不构成读取许可。
- 首批模型服务、地区/账号条件、能力版本及授权消费上限。未具备时继续实现受控契约与恢复测试，真实调用保持未验收。

这些条件不阻塞导航、画布关系与文件导入的独立工作，不通过假内容掩盖缺口。

## PR-1 实施与验证记录

产品源 `597e96f`、最终 PR 提交 `1ac1f87` 通过验收；[PR #42](https://github.com/exo-gravity/scenedesk/pull/42) 于 2026-09-16 合入 `main`，合并提交 `de29b08`。只修改真实项目浏览导航和新画布入口，没有数据迁移、供应商调用或真实文档导入。

- `npm run check` 通过：契约一致性、UI 规则、类型、正式构建和 240 项测试。精确 PR 提交的远端 CI 同时通过 316 项数据库、78 项媒体测试，以及部署和隔离恢复检查。
- 实际构建页面连接本机 API，检查项目默认进入剧本、已有画布入口及原制作页保留后返回、场次管理、资产列表／详情、固定历史剧本阅读、项目导航折叠、820px／390px 抽屉关闭与选择、1440px 明暗主题。正式媒体预览可读取；剧本未提交草稿经新导航离开／返回／恢复后保持准确，恢复原文并刷新无残留，服务器仍为原第 2 版；820px 键盘 Enter 打开抽屉、Escape 关闭并把焦点交回触发按钮。未执行生成、采用或正文更新。
- 初次临时 4315 预览的媒体授权请求被既有来源检查拒绝；验证随后使用匹配 `APP_ORIGIN` 的独立本机 API 4316 和预览 4317，未修改原运行服务的来源检查，媒体恢复可见。相关截图与检查日志保存在本机 `output/implementation/2026-09-16-workspace-navigation/`。浏览器业务数据是已有本地演示，非真实模型验收。
- 初次文档检查受到本工作区未跟踪历史评审中的绝对链接影响；产品源 `597e96f` 的文档校验在只含已提交文件的独立 checkout 中通过：154 个操作、231 个 schema、1,554 处本地链接；报告在该 checkout 的 `output/documentation-checks/2026-09-16-navigation-pr/`。没有更改无关历史草稿以让检查通过。
- 本片不改变服务端权限或持久化，不声称完成新角色／撤权／归档场景的专项业务验收；这类新关系与恢复负例随 PR-2/3 的数据库变化验证。旧制作区内部界面仍保留，项目级画布及最终阅读页尚未交付。

本 PR 不标记整体重构完成。

## 第一轮并行交付状态

导航 [PR #42](https://github.com/exo-gravity/scenedesk/pull/42) 已合入 `de29b08`，E2E 基础 [PR #43](https://github.com/exo-gravity/scenedesk/pull/43) 已合入 `ce456ef`，后者提供 7 条生产 Web／真实受限角色 API 回归。PR #44 项目画布的最终产品头 `590dfc7` 四项 CI 已通过，于 2026-09-16 合入 `432dcfd`。PR #45 已包含同一画布实现并重新生成联合契约，产品头 `d1fff1b` 已通过最终实际浏览器迟到回执与整合画布恢复验收；仍保持草稿。PR #45 的最终 15 条自动 E2E、部署／恢复等远端检查受 GitHub 账户付款／额度限制，任务尚未开始，不能将该状态算作通过。

本轮不关闭八个业务切片：Word 选文直接带入项目画布、飞书正文、连续创作调整、镜头整理、真实模型与团队完整试作仍待后续交付。固定 Word text／revision 已供已有镜头引用接口使用，不代表该新跨页面 UI 已实现。
