# 60 画布完整全览与视口恢复

2026-09-12。修复合法画布内容跨度过大时被10%最小缩放截断，以及坐标极值节点放大后视图无法正确保存的问题。每场画布的内容坐标、权限和版本规则保持既有契约。

## 实际行为

“适应内容”和多选定位使用实际节点边界，最小缩放统一为0.00001，最大缩放仍为4。小比例显示实际百分比，不再四舍五入成0%。远距双节点和2,000节点／5,000引用的全部内容均可进入视口，节点列表支持回到可读尺度；全览本身不保证每个节点文字可读。

屏幕平移量与节点坐标分开：节点仍限±1,000,000，视口平移限±8,000,000，容纳最大4倍缩放后的节点居中。前端只保存与屏幕一致的合法视图，服务端继续执行当前成员权限及独立个人偏好CAS。缩放和恢复不写画布正文或生成任务。

Python合同先行同步生成OpenAPI；追加0096调整缩放、0097区分屏幕平移，两份迁移实际应用后均已冻结。0096 SHA-256为`a411604fe4ae7f0ce230833be8bde3c8e29b35d217510fe6bf4c729d33eff4c7`，0097为`3fca2b72160026050c86c70c0f8b127e4a545a3692729bfa0bb82989869a8529`。不修改已应用历史迁移。

## 验证与限制

整合仓库检查144项通过，包含生成契约、类型、UI约束和生产构建。实际API／受限数据库的视口与画布专项11项通过，覆盖最低与普通缩放、屏幕平移边界、非法节点坐标拒绝、直接SQL约束、旧版本412、私有读取及正文不变。独立代码审查未发现必须修复项。

[生产浏览器证据](../../output/playwright/2026-09-12-canvas-complete-fit/README.md)使用实际API和独立PostgreSQL。远距双节点全览约0.025%，2,000节点全览约1.8%；保存、刷新及分镜模式往返均恢复同一视图。最终正负极值节点在400%下保持中心锚点，等待超过偏好保存间隔后，实际GET与刷新视图一致，四次必要偏好写入全部200，浏览器无页面异常。1512与390宽度已目视检查。

2,000节点全量可见时，本机平移帧间隔P95约67.2ms，仍观察到326.4ms长帧。本片解决内容截断和视图恢复，不宣称容量性能优化完成；300节点的建议性能目标不直接作为2,000节点的达标结论。首次自动化等待与浮点精度断言失败、修复前真实422及后续复验均保留，未覆盖失败记录。真实模型质量和外部部署尚未验收。
