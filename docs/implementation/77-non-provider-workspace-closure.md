# 真实模型前的工作区收尾

2026-09-16。用户要求继续推进，并把真实模型接入放到最后。本轮承接[已确认设计](../design/creative-workspace-approved-2026-09-16.md)和[八个工作包](70-creative-workspace-refactor.md)，不扩大到此前后置的后期剪辑、渲染、运营或公共 API。

## 交付边界

当前保留的用户路径是：外部初稿导入与阅读 → 固定选文和参考进入画布 → 同一草稿中创作、比较、继续修改 → 按需整理镜头并明确选用 → 原片交接。无剧本、无场次时仍允许先在项目画布探索；镜头整理沿用既有场次归属。

Word 和飞书仍采用“预览 → 确认导入 → 当前稿”。历史、下载原件和兼容编辑在次级入口；没有新增分支、发布或审批系统。固定引用、任务与结果留在内部，导入新稿不会改变旧任务依据。

连续创作沿用已有图片／视频草稿、固定任务、结果与比较表示。本轮补助手应用后的“继续编辑此草稿”、被画布边缘遮挡时的完整编辑入口，并把普通路径中的旧分镜台入口收敛到镜头列表。显式旧深链仍保留，不迁移或删除用户已有画布、镜头与候选身份。

镜头交付使用本场当前明确选用的完整原视频，并附有序清单；它不是剪辑渲染。下载前展示清单，准备后再次核对权限、选用与顺序，失败不能交出被当作完整成功的半包。详细契约和边界随[镜头列表](76-shot-list-workspace.md)记录。

## 真实条件与受控验证

- 图像、视频和助手目前用明确标识的测试适配器验证交互、固定输入、结果找回和恢复。供应商服务、凭据、模式、授权额度及真实样片属于最后的真实模型工作包。
- 飞书已提供团队自建应用的正文导入路径。当前本机尚无真实应用配置；合成官方响应能验证站内事务与恢复，不能证明团队真实文档已获授权或已实际导出。配置入口见[75](75-feishu-script-import.md)。
- 本地 Word 样本与受控飞书文档不是团队真实剧本的版式验收。当前承诺为受限 `.docx`，不宣称任意 Office 格式、分页或复杂排版完整保真。
- 本机工作台升级、隔离备份恢复与容器部署检查分别记账；它们不能替代外部生产服务器、真实 OIDC、云权限和团队试用。

## 验证记录

最终联合产品头为 `2923fa1`，包含 main 的 #51／52／53 和两处旧分镜主入口文案调整。该提交之后的收尾文档不改变产品代码。

| 检查 | 本轮实际结果 | 证据 |
|---|---|---|
| 契约、UI 规则、类型、生产构建、单元 | `npm run check` 成功，261/261，无失败／跳过 | closure 工作目录 `output/verification/2026-09-16-closure/final-check.log` |
| 全部生产浏览器用例 | 同一构建 30/30 成功，2.8 分钟，单 worker、零自动重试；含导入／固定引用／同稿创作／选用／交付／冲突与刷新恢复 | closure 工作目录 `output/verification/2026-09-16-closure-final-33232/e2e.log`，截图位于同 runId 的 `output/playwright/` |
| 全量数据库集成 | 41 文件、49 个顶层组，337/337 成功，436.364 秒，无失败／跳过／取消 | regression 工作目录 `output/verification/2026-09-16-final-db-ae1380b-01/database.log` |
| 部署前置 | 配置 8/8；最终数据库审查 12/12，10.852 秒 | closure 的 `deploy-check.log`；regression 同目录 `deploy-database.log` |
| 隔离成对恢复 | 11/11 成功，516.7 秒，退出码 0；实际 PNG 上传／处理、冷存储与数据库成对备份、固定版本恢复、权限和未决任务均验证 | recovery 工作目录 `output/verification/2026-09-16-recovery-diagnostic-02/recovery.tap` 与 `output/engineering/private-recovery/70d09c9d-36c2-493d-99f9-e58dff666357/results.json` |
| 容器部署 | API／Web／worker 三镜像构建成功；完整 smoke 退出码 0，HTTPS、鉴权边界、实际浏览器契约编译与队列拒绝行为通过；独立清理检查通过 | recovery 工作目录 `output/verification/2026-09-16-deployment-82bf9a2/` 的 `README.md`、`results.json`、`smoke.log`、`images.txt` 与 `cleanup.log` |

数据库与部署审查执行于 `ae1380b`，其后 #51 和根收尾没有修改服务端、迁移或数据库契约；最终 30 条浏览器运行于联合产品头，使用真实 API、受限角色与独立合成身份。Word 原件和 ZIP 下载通过浏览器完成并核对字节／摘要；飞书外部响应、供应商和部分媒体归档为明确受控夹具，不能把它们扩展为真实服务验收。所有数据库均为本轮独立创建，测试后清理，未将用户已有项目作为 fixture。

恢复执行头 `82bf9a2` 的运行代码与 `2923fa1` 一致，只为合成 PNG 的失败断言补充受限错误码，便于清理后诊断，未输出媒体记录、文本、对象 key、URL 或凭据。初次恢复在 poster 状态 `failed` 时停止，未进入备份／恢复；当时错误码没有保留，原因未确认。释放其他测试窗口后的完整复验通过，没有放宽解码或容器生命周期限制。本轮专用容器／卷清理后才产出成功结果，并再次核对用户原 PostgreSQL 和 MinIO 仍运行。

部署使用同一 `82bf9a2`，镜像标签为 `closure-82bf9a2`，revision 标签与源一致。真实 Chrome 编译当前公共 schema；同源 readiness、401 业务拒绝、302 安全 OIDC 握手、失效队列提示完成、异常运行提示停止 worker、未支持队列在 claim 前拒绝均通过。真实生成提交保持 `503 GENERATION_EXECUTOR_UNAVAILABLE`，不以启用能力记录冒充执行器已部署。该运行只验证隔离本地容器、TLS、存储与 OIDC discovery，不是实际用户登录或外部云部署。结束后本轮 Compose 项目的容器、卷和网络全部为空，原用户服务保持运行。

### 已合入的专项结果

- [PR #51](https://github.com/exo-gravity/scenedesk/pull/51)，head `d87823a`，合入 `2af36fc`：同一草稿的助手续改、结果比较、旧入口兼容、专注编辑、显式焦点与刷新定位完成。根人工检查发现 390px 按钮裁切后修复，实际复查 390px／820px 和桌面构建；窄屏助手打开／关闭有效。3 条增强专项和最终 30 条套件均通过。
- [PR #52](https://github.com/exo-gravity/scenedesk/pull/52)，head `5ba1566`，合入 `1cbd972`：实际执行原有 25 条浏览器用例，首轮 20 通过、5 失败；定位并修复可访问选择器、跨平台文本选取、显式恢复／保存步骤及媒体 Range 夹具，5 条针对性复验通过。没有放宽产品断言或将测试发现当作运行；最终同构建整套结果另记。
- [PR #53](https://github.com/exo-gravity/scenedesk/pull/53)，head `bbd0042`，合入 `ae1380b`：261 项基础检查、16 项数据库专项、2 条生产浏览器用例通过，覆盖 ZIP 字节摘要、选用变更、撤权、过期、归档、取消和失败清理。根协调实际查看 1280px／390px 清单并确认完整包交给浏览器下载；合成橙／蓝片与归档镜头的排除数量准确。
- 根协调实际播放了固定视频结果（4 秒、`readyState=4`、时间前进、无解码错误），比较另一固定结果后只保留一个暂停的播放器。该证据验证页面与固定媒体读取；归档完成使用明确的受限 SQL 夹具，不能算作真实解码／供应商归档验收。

GitHub Actions 仍因账户付款／额度限制未启动，已读取精确提交的 annotation。合入采用用户已授权的普通 merge 和 head 校验，没有修改保护或使用管理员绕过。

### 环境与未通过的检查

本机完整媒体回归没有通过：在 16 GiB 主机、接近用满的 swap 和高负载下，Docker 的容器创建／清理出现精确 15 秒超时。原先怀疑的嵌入音频用例在独立运行中通过；重跑实际通过生成音频、PCM WAV 等阶段后，又在 `docker create` 被超时终止。保留原失败证据，不放宽生产资源与超时限制。后期音频加工／标准化按当前 MVP 已后置，不能把它作为新增功能，也不能把本轮完整媒体写成全绿。

数据库与核心浏览器用例转到本机已有的 PostgreSQL 16，以单独创建的合成数据库和随机 schema／受限角色运行。该测试实例与用户 4311 工作台的 PostgreSQL、MinIO 完全独立。

## 持久化本机工作台切换

本机 4311 已切换到 `46f201e`，运行产品与联合验证的 `2923fa1` 完全相同；区别只有文档及恢复断言。复用已测试的生产 Web 构建，按新锁文件安装依赖，再用原配置重启 API、Web、媒体 worker 与明确的 assistance fixture worker。没有新迁移、凭据轮换或数据重置；OIDC、原 PostgreSQL／MinIO、原 4343 Word 预览及 4314 原型保持原服务。模型付费执行仍关闭，图像／视频／音频 fixture 也未在用户工作台新启用。

健康检查返回 `businessReady=true`，两个 worker 均 ready。切换前后及浏览器检查后，1 个工作室、2 个项目、2 份剧本修订、2 个场次、1 个画布、13 份画布修订、6 个镜头、7 份候选、7 条选用历史和 1 个生成任务数量一致；没有重新导入剧本、更换选用或创建新画布。

浏览器保留原登录，旧场次链接进入画布，新镜头列表显示 6 个已有选用；读取并确认 4.3 MB 本场原片包，实际 MinIO 原件完成接收并交给浏览器下载。首轮清单在主机高负载时连接超时，显式重试成功；没有改动超时限制或自动提交新的业务意图。该原存储验证与合成夹具逐字节验收分别记录，不把演示视频宣称为真实模型作品。页面最终返回带左侧导航的当前剧本阅读页。

主工作目录的 `output/implementation/2026-09-16-final-workspace-upgrade/` 保存前后计数、运行提交及成功下载截图；私有进程日志与静态构建位于 `.runtime/creative-workspace-main-46f201e/`。临时 4474／4481 预览与独立 native PostgreSQL 已停止；用户已有服务保留。

## 仓库入口与本轮收口

[PR #54](https://github.com/exo-gravity/scenedesk/pull/54)集中整理本轮证据、两处旧分镜主入口文案和安全恢复断言。按用户追加要求，仓库 README、文档总入口、实施包入口及领域术语对齐现行画布工作区；删除当前入口中的“正式工程暂停”“S0 骨架就是当前交付”和“后期继续接入”等过时描述，历史专题与证据保留。GitHub About 已更新为画布主导的短剧视觉创作工作台简介，仓库仍为 private。

当前环境内可完成的非模型实现、自动回归、本机切换、部署包及隔离恢复已收口。余下真实供应商调用按用户要求最后进行；团队飞书授权、实际 Word 版式、外部部署和团队试作仍须相应真实条件，不把受控验收扩大为这些事项已完成。完整媒体回归未通过的环境记录亦保留，不宣称所有历史测试全绿。
