# 镜头列表与固定选用原片下载：第一片

2026-09-16。对应[实施计划](70-creative-workspace-refactor.md) PR-6 的镜头整理部分及[验收矩阵](73-creative-workspace-e2e.md) CW-13/15。基于 PR #45 `459dc36` 并行开发，保留原候选工作台与场次入口；本片不代表 PR-6 或整体重构已完成。

## 产品边界

画布工具栏提供可选「镜头列表」：场次选择、镜头顺序、单镜预览、两份实际视频比较、明确选用、清除选用和下载已选用原片。列表保持 `Shot.sceneId`，不创建新的 Shot 父级，不将画布节点自动变成镜头，不把结果放到画布等同于选用。新增镜头复用原内容创建与恢复流程。没有场次时明确说明先建立归属，不静默补建场次。

选中画布实际视频后，可以从列表登记固定 `Take`，保留媒体、镜头要求、入出点及说明；图片仅作创作参考，不能冒充视频候选。预览其他候选不会改变当前选用。旧要求候选可继续查看，沿用仍走已有候选工作区。历史记录折叠在次级入口，不提供分支、发布或审批式版本管理。

镜头排序提交该场次完整集合，包括已归档镜头，以内容根 CAS 保护并发。归档项目／场次／镜头保持读取能力，禁止新增或选用。关闭弹层、换镜头、换场次前保留未提交输入；保留失败停留并说明。排序失败／刷新通过已有 IndexedDB 草稿明确恢复；冲突时才核对当前集合与版本。候选创建、选用仍复用既有持久化与恢复语义，没有第二套创作状态。

## API 与权限

新增 `POST /v1/tenants/{tenantId}/projects/{projectId}/shots/{shotId}/selection/download`，输入固定 `selectionId`，返回固定 Selection ID、Take、Media 和短时原件下载授权。服务端在同一受限角色事务中核验当前项目权限、镜头当前选用、固定视频媒体及存储对象版本。选用已变化返回 `409 SELECTION_CHANGED`，不会替换为新选择；无 Take 的清除记录不可下载。

这是完整原视频下载，候选区间只随结果说明，文件未裁剪。未新增转码、剪辑、渲染、打包或审批。后续批量交接包仍复用既有交付领域，本片不另建导出任务状态。

下载每次重新鉴权、重新核对选用并签发，不重放缓存中的签名地址。已发出的短时授权仍固定原媒体，不能变成后来选用的另一份视频；权限撤销会拒绝新的访问申请，既有对象存储授权按自身到期时间失效，不声称即时吊销。归档后的历史选用可在仍有权限时下载。`createTake` 和 `reorderContent` 新增缓存前当前归档门禁；撤权也不能通过旧幂等成功响应读取私有对象。本片没有数据库迁移。

## 验证与证据边界

- `npm run check`：246 项单元测试、契约一致性、UI 规则、类型与正式构建通过；最终提交前再次运行，最终结果以 PR 描述为准。
- 专项受限角色 PostgreSQL：`candidates.test.ts`、`content.test.ts`、`shot-list.test.ts`，24 项通过。覆盖完整含归档项排序、CAS、图片拒绝、固定原片与区间、同键重新签发、选用改变拒绝、选用历史不漂移、撤权与归档后缓存重放。
- 初次专项运行发现原候选测试在归档场次之后继续把成功重复候选当撤权前置。已保持既有 `422 INVALID_CANDIDATE` 语义，并在独立撤权用例恢复场次，再验证成功／撤权／拒绝；新增归档缓存负例保留。不是降低权限门禁换取测试通过。
- `tests/e2e/shot-list.spec.ts` 提供三条真实 Web/API 流程：从画布视频建立候选→比较→明确选用→切换预览→下载 SHA-256 核对→含归档子项排序→刷新；排序传输失败后关闭／刷新／恢复及窄屏归档只读；并发选用保留理由、明确核对与撤权缓存隐藏。
- 集成负责人在正式构建 CUA 已确认排序保存后再次编辑、草稿关闭／恢复及蓝／橙候选比较。根据实测，新入口复用编辑器时通过可选 `presentation="list"` 隐藏常态版本号和后期制作／审阅说明，保留固定对象、区间和真实冲突；默认候选工作台不变。首次预览镜头固定身份，排序不再跳转到新的第一条。该小修已通过类型、生产构建，最终页面复验单独记录。
- E2E 使用 checked-in 蓝色／橙色四秒 MP4、隔离 PostgreSQL、真实生产 Web/API 和测试进程内媒体存储；不伪造业务成功响应。视频的已验收元数据直接作为关系 fixture 装载；不证明实际上传探测、对象存储集成或供应商生成通过。排序负例明确只注入一次网络中断。
- 当前 E2E 仅完成类型与测试发现检查（全套 18 条，其中新增 3 条）。[PR #46](https://github.com/exo-gravity/scenedesk/pull/46) 的首轮 CI 四个 job 均因账户付款／额度限制未启动（[verify 记录](https://github.com/exo-gravity/scenedesk/actions/runs/35070003070)），不是测试失败或通过。实际浏览器与视觉由根任务统一 CUA 验收后补证据，不能把 `--list` 当通过。

## 独立生产预览

`tests/e2e/preview-shot-list.ts` 只用于本机验收，不被产品入口引用。先 `npm run build`，使用专用 loopback `drama_e2e*` 数据库、`PROVIDER_MODE=mock` 启动。默认 Web 为 `[::1]:4464`，与常见 `127.0.0.1` / `localhost` 会话 cookie 分离；现有 E2E 默认 host 仍为 `127.0.0.1`。runner 打印一个临时 loopback 入口，打开后以 HttpOnly Cookie 进入合成身份，不打印会话值。Ctrl-C 停止服务器、移除专用 schema 与角色。不要指向已有业务数据库。

本片尚未完成：真实视频生成验收、完整连续创作／助手集成、批量按选用交接包、团队实际镜头组验收以及重复工作台入口移除。仅在替代能力完成后再删除旧入口。
