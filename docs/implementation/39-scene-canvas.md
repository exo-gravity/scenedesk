# 场次画布：实施记录

当前范围依据[38 当前 MVP](38-first-release-scope-review.md)。画布与 AI 保留原方案，后期处理不再是发布依赖。GitHub 组织于 2026-09-11 按用户变更改为 `exo-gravity`，当前仓库为 [exo-gravity/scenedesk](https://github.com/exo-gravity/scenedesk)，已有 [PR #16](https://github.com/exo-gravity/scenedesk/pull/16) 延续。本文记录 `feat/scene-canvas` 的实际进展；已接通 CX01 的服务基础和部分 CX02／CX06 页面恢复，尚不是完整画布业务交付。

## 已实现的服务基础

接通 `ensureSceneCanvas`、`getSceneCanvas`、`getCanvas`、`saveCanvas`、`getCanvasRevision`、`listCanvasHistory`、`getSceneWorkspacePreference`、`saveSceneWorkspacePreference` 八个既定操作。读取不创建画布或偏好；明确进入时并发请求仍只创建一张场次画布。画布引用真实场次，所有访问使用当前项目授权、会话和 CSRF 检查，幂等重放仍重新授权。

迁移 0017 增加通用画布根、场次接入、不可变修订、节点身份墓碑、类型化媒体／资产引用、私人视图偏好及同事务失效通知记录。画布文档不依赖镜头和剪辑。画布正文采用独立的类型化 `canvas_history_bodies`，复用 `editing-json-v1` 编码与现有有限历史策略，避免改写已应用的 Cut 外键与历史正文。正文按 hash 去重，数据库校验实际 SHA-256；修订只能在对象锁内追加，当前和前一版不能删除。未来真正需要完整修订的业务引用须加入类型化 pin；生成来源另存确切输入，不能默认把所有完整历史永久保留。

整个文档按 If-Match 保存；冲突返回 412 和当前修订，相同内容重送不追加伪历史，但仍核对引用权限。接受不完整提示和模型选择草稿，不执行模型。文字、媒体、草稿节点、引用边和分组遵循原数量／UTF-8 字节上限；引用只能从文字或现有媒体进入草稿。节点种类和媒体身份一经建立不可替换，删除后恢复仍检查墓碑。原有且仍授权的归档素材可在当前节点保留／移动，复制或重新引入需可用素材。固定资产版本必须实际包含所引用媒体；不能用外项目素材伪造引用。

历史列表使用用户／项目／画布绑定的游标，并按当前保留策略过滤。超过保留期返回 410；保存时在同一对象锁内清理修订、无引用正文及其类型化投影，节点身份不随历史清理消失。个人模式、选中和视口使用独立 CAS，首次版本为 0，不增加公共画布修订；RLS 限制只能读取本人偏好。

## 验证证据

本地 `npm run check` 通过契约、UI 规则、类型、生产构建和 41 项单元测试。完整 PostgreSQL 回归 108 项通过，无失败、取消或跳过；之后加入容量边界后，画布专项 10 项全部通过。覆盖读取无写入、并发首次进入、同版本保存竞争、幂等重送、历史分页、有限清理、文字／草稿及关系校验、固定媒体墓碑、归档引用、跨项目拒绝、私人偏好首次及后续 CAS、成员撤权后缓存重放拒绝、受限 SQL 写保护。

同一专项的真实 API 保存并重读耗时：300 节点／500 边 223 ms；2,000 节点／5,000 边 1,197 ms；2,000 个已授权媒体节点 6,148 ms。103 次连续保存和历史清理约 21.3 秒。这些是本机单次关系夹具的服务耗时，不是浏览器交互 P95、并发容量或媒体解码验收；媒体字节验收沿用既有真实媒体套件，不把本测试的关系夹具说成真实素材导入。

首轮偏好测试因夹具遗漏项目成员 ID 失败，修正夹具后完整回归通过。代码复核同时修正后续偏好保存路径，使用显式 UPDATE 避免首次 INSERT 约束被 upsert 再次触发，并增加后续保存断言。

## 页面与共享恢复接入（2026-09-11）

React Flow 12.11.6 已进入真实场次制作路由，与分镜共用场次上下文。画布支持文字、图像／视频／声音草稿、已有素材节点、参考用途及启停、位置／宽度、复制、分组、列表查找、缩放定位与会话内撤销／重做。参数与生成入口仍等待实际能力描述；当前未创建模型任务。已接通的页面保留明确创建动作，不以打开页面隐式新增业务对象。素材和历史按需停靠；选中时压缩画布高度，为中央有界输入保留空间，沿用已确认主题。

`EditingDocumentController` 统一工作稿与画布的单请求保存、持久待核对请求、本机 CAS、恢复和访问生命周期；Cut 适配器保留原有本机记录字段，Canvas 使用真实独立格式。模式切换不卸载正在保存的画布会话。坐标和空名称的未完成输入存入本机缓冲，不写成非法服务文档；恢复后定位待补全节点。相同已保存内容的后台读取不清空撤销记录，逐次引用编辑只调整对应草稿的引用序位，不因布局保存重新排序其他草稿输入。

冲突界面固定比较快照，选定对象才重放，同时保留未选的服务端新增／修改。比较中再次变化会保留勾选但暂停应用，重新读取后重新选择。历史预览展示正文、位置和引用，取回按钮绑定打开时的当前草稿；取回创建新修订，可撤销回取回前内容。当前历史查询随成功保存失效刷新。收到访问提示时先隐藏画布并重新核对，拒绝后清理本机内容；断网不等于撤权。视频／音频仅点击后挂载，另开预览或切模式释放原播放器。

### 本次验证

- `npm run check` 通过契约、UI 规则、类型、生产构建和 44 项单元测试（包含选定对象重放、关联删除和引用序位测试）。服务基础提交 `cac3b7e` 的 push／PR 两组 CI 已成功；PR 实际日志为 41 单元、109 数据库、61 媒体，无失败或取消。新增前端提交的 CI 单独跟踪，不挪用前一提交结论。
- 原工作稿九类恢复回归通过；共享访问八类回归通过，覆盖旧本机记录、在途新输入、迟到回包、二次冲突、存储失败、精确对象／会话隔离和晚到退出清理。画布控制器单独验证撤销合并、同内容读取、未完成数值、未知已提交请求、二次冲突和撤权。
- 生产构建连接真实本地 API／PostgreSQL，实际触发 412、保存成功后丢回包，以及阻断网络后刷新。显式重放保留同伴新增内容；GET 核对成功的保存没有重复 PUT；本机恢复先取得当前授权，未完成坐标与空名称均能跨模式／刷新找回并修正保存。
- 实际四秒技术视频可解码播放，播放器控件不拖动画布；同时只保留一个播放器，切分镜释放，返回不自动播放。1512×982、1366×900、760×900、390×844 无页面横向溢出；桌面当前输入在视口内，最终截图已人工查看。窄屏是列表模式，不是完整移动制作验收。
- 历史实测从 r22 经修改到 r24，查看正文后取回为 r25，再撤销为 r26；正文 hash 分别等于选定历史与取回前版本。打开预览后修改本机内容会阻止旧确认。生产页面的跨标签页访问提示先隐藏内容，再由当前 GET 决定恢复或拒绝。

[浏览器结果](../../output/playwright/scene-canvas/verification-results.json)、[真实网络恢复脚本](../../output/playwright/scene-canvas/verify-page-recovery.js)、[媒体与布局脚本](../../output/playwright/scene-canvas/verify-media-layout.js)、[历史脚本](../../output/playwright/scene-canvas/verify-history.js)、[访问脚本](../../output/playwright/scene-canvas/verify-access.js)、[桌面截图](../../output/playwright/scene-canvas/09-media-layout-v2-1512.png)、[1366 截图](../../output/playwright/scene-canvas/09-media-layout-v2-1366.png)、[历史预览](../../output/playwright/scene-canvas/12-reviewed-history-v2.png)。这些仍为本地技术夹具，不构成真实主创任务或模型质量验收。

本次发现并修正了撤销被同内容轮询清空、未完成数值／空名称恢复时焦点不对应、历史预览位于长列表底部和访问复核期间仍展示内容的问题。测试初轮还遇到本地服务随同组退出、Vite 热更新引起测试直引 `ApiError` 与业务导入版本不同，以及比较刷新／重复夹具／详情选择器等待条件问题；恢复服务、刷新模块图并校正测试后通过，未以放宽产品授权处理来消除测试失败。

## 继续接入

镜头参考／候选绑定、本场独立探索入口、编辑者提示、通知投递、拖入上传／继续创作／多选参考、模型能力参数与固定生成计划／结果取回仍待接通，分组与快捷键还需扩大实际场次走查。现阶段场次读取的 bindings 为空；没有开放虚假绑定／生成操作。300／2,000 节点的浏览器交互、帧时、内存和媒体容量尚未验收。AI 服务仍未配置，完整流程完成后还需要真实模型及实际场次验收，不能以本批页面宣布画布或 MVP 完成。PR #16 保持草稿，后续继续完成绑定等首个可用业务切片。
