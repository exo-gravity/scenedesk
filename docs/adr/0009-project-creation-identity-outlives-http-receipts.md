# 项目创建身份独立于短期请求回执

新建项目没有可用于防止重复创建的已有内容版本；只延长 HTTP 回执有效期仍会留下过期后的重复窗口。因此携带 `creationRequestId` 的创建由持久绑定记录裁决：同一工作室、创建者和原请求只对应一个项目，绑定与项目初始化同事务保存，并随项目保留。HTTP 请求键仍遵守 24 小时内不能用于不同输入的规则，但恢复结果从持久绑定读取，不回放过时的项目表示；当前权限重新检查，更名或归档不会被原创建输入覆盖，旧客户端及其他操作的缓存协议保持原状。
